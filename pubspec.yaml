name: pubcash_mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  # J'ai supprimé la ligne "share_plus" qui était mal placée ici

  # UI & Design
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  lottie: ^3.0.0
  
  # State Management
  provider: ^6.1.1
  
  # Network
  http: ^1.1.2
  dio: ^5.4.0
  
  # Storage
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  
  # Validation
  email_validator: ^2.1.17
  intl: ^0.18.1
  
  # UI Components
  flutter_spinkit: ^5.2.0
  fluttertoast: ^8.2.4
  
  # Social Auth
  google_sign_in: ^6.1.6
  flutter_facebook_auth: ^6.0.3
  url_launcher: ^6.2.1
  
  # Formulaires
  dropdown_search: ^5.0.6
  
  # Vidéo & Média
  video_player: ^2.8.7
  chewie: ^1.8.1
  cached_network_image: ^3.3.1
  share_plus: ^7.2.2  # Il est bien placé ici (à la fin)
  image_picker: ^1.2.1
  flutter_fortune_wheel: ^1.3.2
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/animations/